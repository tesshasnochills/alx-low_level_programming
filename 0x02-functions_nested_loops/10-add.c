#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first integer 
 * @b: first integer
 *
 * Return: the value of the sum of @i and @j
 */
int add(int a, int b)
{
  return(a + b);
}
