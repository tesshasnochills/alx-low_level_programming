#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first integer
 * @b: first integer
 *
 * Return: the value of the sum of @i and @j
 * 
 * FUNCTIONALITY *
 * 
 * 1. The first line is a preprocessor command telling it to access the contents of the header file.
 * 
 * 2. A function called add is declared and takes 2 integers as arguments and returns an integer.
 * 
 * 3. The value of a + b is returned.
 */
int add(int a, int b)
{
return (a + b);
}
